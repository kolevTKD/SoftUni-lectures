function solve (studentName, studentAge, avgGrade) {
    let studentInfo = `Name: ${studentName}, Age: ${studentAge}, Grade: ${avgGrade.toFixed(2)}`

    console.log(studentInfo);
    
}

solve ('John', 15, 5.54678)